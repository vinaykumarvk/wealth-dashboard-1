import React, { forwardRef } from 'react';
import { useSelector } from 'react-redux';
import { formatCurrency, formatPercentage } from '../utils/formatters';

// PDFContent.js is removed as per the intention.

export default null;